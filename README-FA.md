# 🦸 Hero Wallet - Multi-Chain Browser Extension Wallet

یک کیف پول چندزنجیره‌ای کاملاً کاربردی برای مرورگر که از Ethereum, Bitcoin, Solana, TON, Sui و Hedera پشتیبانی می‌کند.

## ✨ ویژگی‌های اصلی

### 🔐 امنیت و مدیریت حساب

- ✅ **رمزگذاری قوی**: استفاده از AES-256-GCM و PBKDF2 با 200,000 تکرار
- ✅ **مدیریت PIN**: قفل خودکار بعد از 5 دقیقه و امکان ذخیره PIN (اختیاری)
- ✅ **حساب‌های چندگانه**: پشتیبانی از چندین حساب در هر بلاکچین
- ✅ **BIP39/BIP44**: استفاده از استانداردهای صنعت برای مدیریت کلیدها

### 🌐 پشتیبانی چندزنجیره‌ای

- **Ethereum (EVM)**: Ethereum, Polygon, BSC, و سایر شبکه‌های سازگار با EVM
- **Bitcoin**: پشتیبانی از آدرس‌های Native SegWit (Bech32)
- **Solana**: تراکنش‌های سریع و کم‌هزینه
- **TON**: بلاکچین باز Telegram
- **Sui**: بلاکچین نسل بعدی برای dApp ها
- **Hedera**: شبکه توزیع‌شده سرعت بالا

### 🔗 قابلیت‌های Web3

- ✅ **اتصال به dApp ها**: پشتیبانی کامل از `eth_requestAccounts`
- ✅ **امضای پیام**: `personal_sign`, `eth_sign`, `eth_signTypedData` (EIP-712)
- ✅ **ارسال تراکنش**: `eth_sendTransaction` با تایید کاربر
- ✅ **تعویض شبکه**: `wallet_switchEthereumChain`
- ✅ **RPC Forwarding**: اتصال خودکار به RPC برای متدهای فقط خواندنی

### 🎨 رابط کاربری

- 🌓 **تم تیره/روشن**: تعویض آسان بین تم‌ها
- 📱 **طراحی Responsive**: برای تمام اندازه‌های صفحه
- ⚡ **سریع و سبک**: بدون وابستگی‌های غیرضروری
- 🎯 **UX ساده**: مناسب برای مبتدیان و حرفه‌ای‌ها

## 🚀 نصب و راه‌اندازی

### پیش‌نیازها

```bash
Node.js >= 18
npm >= 9
```

### مراحل نصب

1. **Clone کردن repository**

```bash
git clone https://github.com/ericdevteam-ops/inject-hero-wallet.git
cd inject-hero-wallet
```

2. **نصب وابستگی‌ها**

```bash
npm install
```

3. **بیلد افزونه**

```bash
npm run build
```

4. **بارگذاری در مرورگر**
   - Chrome/Edge: `chrome://extensions/`
   - Firefox: `about:debugging#/runtime/this-firefox`
   - فعال‌سازی "Developer mode"
   - کلیک روی "Load unpacked" و انتخاب پوشه `dist`

## 📖 نحوه استفاده

### اولین راه‌اندازی

1. پس از نصب، روی آیکون افزونه کلیک کنید
2. PIN خود را وارد کنید (پیش‌فرض: `123456`)
3. اختیاری: گزینه "Remember PIN" را علامت بزنید
4. حساب‌های شما به‌صورت خودکار از seed ساخته می‌شوند

### اتصال به یک dApp

1. به یک dApp سازگار با Web3 بروید (مثل Uniswap)
2. روی "Connect Wallet" کلیک کنید
3. Hero Wallet را انتخاب کنید
4. درخواست اتصال را تایید کنید
5. آماده‌اید! 🎉

### امضای تراکنش

1. در dApp یک عملیات انجام دهید
2. پنجره تایید Hero Wallet باز می‌شود
3. جزئیات تراکنش را بررسی کنید
4. "Approve" یا "Reject" کلیک کنید

## 🛠️ توسعه

### ساختار پروژه

```
src/
├── background.ts          # Service worker اصلی
├── contentScript.ts       # پل بین page و extension
├── inpage.ts              # Provider تزریق‌شده در صفحه
├── adapters/              # آداپترهای بلاکچین
│   ├── evm.ts            # Ethereum و شبکه‌های EVM
│   ├── btc.ts            # Bitcoin
│   ├── solana.ts         # Solana
│   ├── ton.ts            # TON
│   ├── sui.ts            # Sui
│   └── hedera.ts         # Hedera
├── utils/
│   ├── keystore.ts       # مدیریت کلیدها و session
│   ├── crypto.ts         # عملیات رمزنگاری
│   ├── networks.ts       # تنظیمات شبکه
│   └── approvalHandler.ts # پردازش درخواست‌های تایید‌شده
├── popup/                # رابط کاربری popup
└── ui/                   # صفحات تایید و تنظیمات
```

### اسکریپت‌های دسترسی

```bash
# توسعه با watch mode
npm run dev

# بیلد تولید
npm run build

# پیش‌نمایش (بعد از build)
npm run preview
```

### اضافه کردن شبکه جدید

1. تنظیمات شبکه را در `src/utils/networks.ts` اضافه کنید
2. اگر نیاز است، آداپتر جدید در `src/adapters/` بسازید
3. منطق اتصال را در `src/background.ts` به‌روز کنید

## 🔧 تنظیمات پیشرفته

### تغییر مدت‌زمان قفل

در `src/utils/keystore.ts`:

```typescript
private readonly DEFAULT_LOCK_TIME = 5 * 60 * 1000; // 5 دقیقه
```

### اضافه کردن RPC سفارشی

در `src/background.ts`:

```typescript
const NETWORKS = {
  '0x1': { chainId: '0x1', rpc: 'YOUR_RPC_URL', name: 'Network Name' },
  // ...
};
```

### تعویض Mnemonic پیش‌فرض

⚠️ **هشدار**: فقط برای توسعه!

در `src/utils/accountSeed.ts`:

```typescript
const DEFAULT_MNEMONIC = 'your twelve word seed phrase here';
const DEFAULT_PIN = 'your-pin';
```

## 🧪 تست

### تست با dApp های محبوب

- ✅ **Uniswap**: https://app.uniswap.org
- ✅ **OpenSea**: https://opensea.io
- ✅ **Aave**: https://app.aave.com
- ✅ **PancakeSwap**: https://pancakeswap.finance

### تست Manual

```bash
# فایل تست MetaMask
# بازکردن test-eip6963.html در مرورگر
open test-eip6963.html
```

## 🔒 امنیت

### بهترین شیوه‌ها

- ✅ هرگز private key یا mnemonic خود را به اشتراک نگذارید
- ✅ فقط به سایت‌های معتبر متصل شوید
- ✅ همیشه جزئیات تراکنش را قبل از تایید بررسی کنید
- ✅ از رمزهای قوی استفاده کنید
- ✅ به‌طور منظم از wallet خود backup بگیرید

### ویژگی‌های امنیتی

- 🔐 رمزنگاری کلیدها با AES-256-GCM
- 🔐 Key derivation با PBKDF2 و 200,000 تکرار
- 🔐 Session management با قفل خودکار
- 🔐 Secure memory wiping برای داده‌های حساس
- 🔐 Content Security Policy سخت‌گیرانه

## 📝 متدهای پشتیبانی‌شده

### Ethereum JSON-RPC

- ✅ `eth_requestAccounts` - اتصال حساب
- ✅ `eth_accounts` - لیست حساب‌های متصل
- ✅ `eth_chainId` - شناسه شبکه فعلی
- ✅ `personal_sign` - امضای پیام
- ✅ `eth_sign` - امضای داده خام
- ✅ `eth_sendTransaction` - ارسال تراکنش
- ✅ `wallet_switchEthereumChain` - تعویض شبکه
- ✅ `wallet_addEthereumChain` - اضافه کردن شبکه
- ✅ `eth_getBalance` - دریافت موجودی
- ✅ `eth_call` - فراخوانی contract
- ✅ `eth_estimateGas` - تخمین gas
- ✅ و متدهای فقط خواندنی دیگر...

## 🤝 مشارکت

مشارکت‌ها استقبال می‌شوند! لطفاً:

1. Fork کنید
2. Branch جدید بسازید (`git checkout -b feature/amazing-feature`)
3. تغییرات را commit کنید (`git commit -m 'Add amazing feature'`)
4. Push کنید (`git push origin feature/amazing-feature`)
5. Pull Request باز کنید

## 📄 لایسنس

این پروژه تحت لایسنس MIT منتشر شده است.

## 🙏 تشکر

- [ethers.js](https://ethers.org/) - کتابخانه Ethereum
- [bitcoinjs-lib](https://github.com/bitcoinjs/bitcoinjs-lib) - کتابخانه Bitcoin
- [@solana/web3.js](https://solana-labs.github.io/solana-web3.js/) - Solana SDK
- [@mysten/sui.js](https://github.com/MystenLabs/sui) - Sui SDK
- [TweetNaCl](https://tweetnacl.js.org/) - رمزنگاری

## 📞 پشتیبانی

اگر مشکلی پیش آمد:

1. [Issues](https://github.com/ericdevteam-ops/inject-hero-wallet/issues) را بررسی کنید
2. Issue جدید باز کنید
3. به ما در [Discussions](https://github.com/ericdevteam-ops/inject-hero-wallet/discussions) بپیوندید

---

**ساخته شده با ❤️ توسط تیم Eric Dev**

⭐ اگر این پروژه برایتان مفید بود، یک ستاره بدهید!
