# 🧪 راهنمای تست کیف پول Hero Wallet

این راهنما مراحل کامل تست افزونه Hero Wallet را شرح می‌دهد.

## 📋 پیش‌نیازها

1. ✅ پروژه را build کرده باشید:

   ```bash
   npm run build
   ```

2. ✅ پوشه `dist` ایجاد شده باشد

## 🚀 نصب افزونه در مرورگر

### گام 1: باز کردن Extensions

1. Chrome یا Edge را باز کنید
2. به آدرس `chrome://extensions/` بروید
3. **Developer mode** را در گوشه بالا سمت راست فعال کنید

### گام 2: بارگذاری افزونه

1. روی دکمه **"Load unpacked"** کلیک کنید
2. پوشه `dist` را از مسیر پروژه انتخاب کنید
3. افزونه Hero Wallet باید در لیست نمایش داده شود

### گام 3: اولین راه‌اندازی

1. روی آیکون افزونه در toolbar کلیک کنید
2. اگر کیف پول جدید است:

   - روی "Create Wallet" کلیک کنید
   - یک **Mnemonic Phrase** (12 کلمه) نمایش داده می‌شود
   - آن را یادداشت کنید (بسیار مهم! 🔐)
   - یک **PIN** (4-8 رقم) وارد کنید
   - مجدداً PIN را تأیید کنید

3. اگر کیف پول دارید:
   - روی "Restore Wallet" کلیک کنید
   - Mnemonic خود را وارد کنید
   - یک PIN جدید تعیین کنید

## ✅ تست با صفحه تست داخلی

### مرحله 1: باز کردن صفحه تست

1. فایل `test-hero-wallet.html` را در مرورگر باز کنید
2. صفحه تست با UI زیبا نمایش داده می‌شود

### مرحله 2: اتصال کیف پول

1. **مهم**: ابتدا popup افزونه را باز کنید و PIN را وارد کنید (این کار wallet را unlock می‌کند)
2. در صفحه تست روی **"Connect Wallet"** کلیک کنید
3. پنجره approval باز می‌شود
4. روی **"Approve"** کلیک کنید
5. در Activity Log باید پیام "Connected" را ببینید
6. آدرس حساب و Chain ID نمایش داده می‌شود

### مرحله 3: تست امضای پیام (Sign Message)

1. در بخش "Sign Message" یک پیام وارد کنید (مثلاً: "Hello Hero Wallet!")
2. روی **"Sign Message"** کلیک کنید
3. پنجره approval باز می‌شود و پیام را نشان می‌دهد
4. روی **"Approve"** کلیک کنید
5. Signature در صفحه نمایش داده می‌شود
6. می‌توانید روی **"Verify Signature"** کلیک کنید

### مرحله 4: تست ارسال تراکنش

⚠️ **هشدار**: برای تست واقعی حتماً از شبکه **Sepolia Testnet** استفاده کنید!

1. ابتدا به Sepolia تغییر شبکه دهید:

   - روی دکمه **"Sepolia"** در بخش "Switch Network" کلیک کنید

2. مطمئن شوید که موجودی Sepolia ETH دارید:

   - می‌توانید از [Sepolia Faucet](https://sepoliafaucet.com/) استفاده کنید

3. در بخش "Send Transaction":

   - **To Address**: آدرس مقصد را وارد کنید
   - **Amount**: مقدار (مثلاً 0.001)
   - روی **"Send Transaction"** کلیک کنید

4. پنجره approval باز می‌شود با جزئیات تراکنش
5. روی **"Approve"** کلیک کنید
6. Transaction Hash نمایش داده می‌شود

### مرحله 5: تست تغییر شبکه

1. روی یکی از دکمه‌های شبکه کلیک کنید:

   - **Ethereum** (Mainnet)
   - **Polygon**
   - **BSC**
   - **Sepolia** (Testnet)

2. Chain ID در بالای صفحه تغییر می‌کند
3. موجودی برای شبکه جدید به‌روز می‌شود

## 🌐 تست با dApp های واقعی

### تست با Uniswap (Sepolia)

1. به [Sepolia Uniswap](https://app.uniswap.org/) بروید
2. روی "Connect Wallet" کلیک کنید
3. Hero Wallet را انتخاب کنید (باید در لیست باشد)
4. Connection را approve کنید
5. سعی کنید یک Swap انجام دهید (روی Sepolia)

### تست با OpenSea Testnet

1. به [OpenSea Testnet](https://testnets.opensea.io/) بروید
2. Wallet را connect کنید
3. NFT های خود را مشاهده کنید

### تست با سایت‌های دیگر

هر سایتی که از MetaMask پشتیبانی می‌کند، باید Hero Wallet را هم شناسایی کند.

## 🔍 Debugging

### بررسی لاگ‌ها

#### 1. Console لاگ صفحه وب

- F12 → Console
- لاگ‌های `inpage.js` و `contentScript.js` را ببینید

#### 2. Background Service Worker

- `chrome://extensions/` → Hero Wallet → "service worker"
- لاگ‌های `background.ts` را ببینید

#### 3. Popup DevTools

- روی popup راست‌کلیک کنید → "Inspect"
- لاگ‌های `popup.tsx` را ببینید

### مشکلات رایج و راه‌حل

#### ❌ "Wallet not initialized"

**علت**: Wallet unlock نشده است
**راه‌حل**: Popup را باز کنید و PIN را وارد کنید

#### ❌ "User rejected request"

**علت**: در پنجره approval روی Reject کلیک کرده‌اید
**راه‌حل**: دوباره تلاش کنید و Approve کنید

#### ❌ "Insufficient funds"

**علت**: موجودی کافی ندارید
**راه‌حل**:

- در Sepolia: از Faucet استفاده کنید
- در Mainnet: ETH واقعی نیاز است

#### ❌ افزونه تشخیص داده نمی‌شود

**راه‌حل**:

1. صفحه را refresh کنید (F5)
2. Browser را restart کنید
3. افزونه را remove و دوباره load unpacked کنید

#### ❌ خطای TypeScript

**راه‌حل**:

```bash
npm run build
```

اگر خطا داد، فایل‌های backup را حذف کنید و دوباره build کنید.

## 📊 چک‌لیست تست

- [ ] نصب افزونه در Chrome
- [ ] ایجاد wallet جدید با Mnemonic
- [ ] تنظیم PIN
- [ ] فعال کردن "Remember PIN"
- [ ] باز کردن test-hero-wallet.html
- [ ] Connect کردن wallet
- [ ] Sign کردن یک پیام
- [ ] Verify کردن signature
- [ ] تغییر به Sepolia Testnet
- [ ] دریافت Sepolia ETH از Faucet
- [ ] ارسال یک تراکنش تست
- [ ] تست تغییر شبکه (Ethereum, Polygon, BSC)
- [ ] تست با Uniswap Testnet
- [ ] تست با OpenSea Testnet
- [ ] تست wallet_getPermissions
- [ ] تست wallet_revokePermissions

## 🎯 نکات مهم

1. **همیشه از Testnet استفاده کنید**: برای تست، حتماً Sepolia یا Goerli را انتخاب کنید
2. **Mnemonic را نگه دارید**: این 12 کلمه تنها راه بازیابی wallet شماست
3. **PIN را یادداشت کنید**: اگر PIN را فراموش کردید، باید wallet را restore کنید
4. **Auto-lock**: بعد از 5 دقیقه، wallet قفل می‌شود
5. **Remember PIN**: اگر فعال باشد، PIN روی دستگاه ذخیره می‌شود (رمزنگاری شده)

## 📸 Screenshot ها

### Popup (Unlocked)

- لیست حساب‌ها
- آدرس EVM
- دکمه Copy Address
- دکمه Lock

### Approval Window

- اطلاعات سایت درخواست‌کننده
- جزئیات تراکنش یا پیام
- دکمه‌های Approve/Reject
- پیام هشدار امنیتی

### Test Page

- وضعیت Connection
- فرم Sign Message
- فرم Send Transaction
- دکمه‌های Switch Network
- Activity Log

## 🛠️ توسعه بیشتر

اگر می‌خواهید قابلیت‌های بیشتری اضافه کنید:

1. **نمایش موجودی توکن‌ها**
2. **تاریخچه تراکنش‌ها**
3. **مدیریت چندین حساب**
4. **Address Book**
5. **تنظیمات گس (Gas)**
6. **نمایش NFT ها**

## 📞 پشتیبانی

اگر مشکلی پیدا کردید:

1. لاگ‌ها را چک کنید (Console + Service Worker)
2. افزونه را reload کنید
3. Browser را restart کنید
4. Issue در GitHub باز کنید

---

**موفق باشید! 🚀**
